spring:
  application:
    # 服务名（pom文件<artifactId>xxx</>中的值）
    name: @project.artifactId@
  cloud:
    nacos:
      discovery:
        # nacos注册中心地址
        server-addr: localhost:8848
server:
  port: 9000
